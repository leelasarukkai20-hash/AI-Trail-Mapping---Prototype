# Template showing which environment variables the scripts need.
# Copy this file to ".env" and fill in real values. Safe to commit (no secrets here).

# Mapbox public token (starts with pk.) — used by scaffold.ts to backfill
# elevation for GPX tracks that have none. Not required if your tracks already
# include elevation.
MAPBOX_TOKEN=
